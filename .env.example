# 项目路径配置
PJ_MD="D:\Developer\g3\dpackweb\BACKEND-MD"
PJ_SD="D:\Developer\g3\dpackweb\BACKEND-SD" 
PJ_YIMM="D:\Developer\g3\dpackweb\BACKEND-YIMM"

# 排除目录配置 - 方法1：一行式（简单）
EXCLUDE_DIRS=node_modules,.git,.idea,__pycache__,.vscode,.DS_Store,dist,build,.env,.venv,venv,env,.pytest_cache,.mypy_cache,.coverage

# 排除目录配置 - 方法2：分类式（可读性更好）
# EXCLUDE_DIRS_DEPS=node_modules,__pycache__,.pytest_cache,.mypy_cache
# EXCLUDE_DIRS_VCS=.git,.idea,.vscode,.DS_Store  
# EXCLUDE_DIRS_BUILD=dist,build,.env,.venv,venv,env,.coverage

# 说明：
# - 如果只设置 EXCLUDE_DIRS，程序会使用该变量
# - 如果设置了分类变量（EXCLUDE_DIRS_*），程序会合并所有分类
# - 两种方式可以同时使用，程序会自动去重
